<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljh.dao.CourseMapper">
    <insert id="addCourse" parameterType="Course">
        insert into course (课程名称,学分) values(#{CourseName},#{Credit})
    </insert>
    <delete id="deleteCourse" parameterType="int">
        delete from course where 班级号=#{id}
    </delete>
    <update id="updateCourse" parameterType="Course">
        update course set 课程名称=#{CourseName},学分=#{Credit} where 课程号=#{CourseId}
    </update>
    <select id="queryCourseById" parameterType="int" resultType="Course">
        select * from course where 课程号=#{id}
    </select>
    <select id="queryCourseAll" resultType="Course">
        select * from course
    </select>
</mapper>